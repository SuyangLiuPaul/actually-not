# 路线图 · 「其实不是」这个站

这份只讲**现在这个站**要做什么。按建议顺序排的。

> **另有一份 [`FUTURE-APPS.md`](FUTURE-APPS.md)**，那是以后要做的一系列**其他**站点的规划。
> **现在不要动它，也不要为了它改这个站的代码。** 列在那里只是为了不丢，
> 等这份路线图推进到一定程度再说。这份文档里的任何一项都不依赖那份。

工作量标记：`S` 半天内 · `M` 一两天 · `L` 三天以上。

---

## P0 · 先把手上的东西发出去 `S`

- [x] **提交并发布未提交的改动**（✅ 已发布：`1d7b043` + `0fcedd2`，线上已验证）

工作区里有一批已完成但没提交的工作：

- 内容从 55 条涨到 **61 条**
- 已读标记（`src/hooks/useReadProgress.ts`）
- 详情页分享按钮（系统分享 + 复制链接兜底）
- 搜索词高亮
- FAQ JSON-LD 结构化数据
- 弹层内 Tab 焦点圈定、关闭后焦点归位
- `?random=1` PWA 快捷方式

`npm run check` 是绿的（**676 条断言**通过）。先跑一遍检查，然后提交推送，
别让它一直悬在工作区里——万一误操作就全没了。

```bash
npm run check
git add -A
git commit -m "内容扩到 61 条，加已读标记、分享、搜索高亮、结构化数据"
git push origin main
```

---

## A1 · 让它能被搜到 `L` ★ 最重要，且要最先做 —— ✅ 已完成（`3ad03d1`）

**这是目前最大的结构性缺口。** 站点用的是 hash 路由（`#/wet-hair`），
搜索引擎实际上只看得到**一个**页面。已经加的 FAQ JSON-LD 是补丁不是解药——
它帮爬虫理解内容，但每一条仍然没有属于自己的、可索引的 URL。

- [x] 路由从 hash 改成真实路径：`/wet-hair`
- [x] 构建时为每条预渲染静态 HTML（用 vite-ssg，或自己写脚本跑 61 次 `renderToString`）
- [x] 每页注入自己的 `<title>` / `meta description` / `canonical`
- [x] 生成 `sitemap.xml` 和 `robots.txt`
- [x] **旧的 `#/xxx` 链接要重定向到新路径**——已经分享出去的链接不能断

**为什么排第一**：61 条内容 = 61 个可被搜索到的页面，这是这个站能不能长出
自然流量的分水岭。而且它动的是路由层，**越晚做改动面越大**，后面的
A3（每条分享图）也依赖它。

> ⚠️ 改成真实路径之后，`public/_redirects` 里的 SPA 回退就从"额外保险"
> 变成"必需品"了。同时记得 `netlify.toml` 也要同步改（见 HANDOVER 坑③）。

---

## A2 · 测验模式「你中了几条」 `M` ★ 传播杠杆

- [ ] 随机抽 10 条，只显示「你以为」那句话
- [ ] 让用户选「我信过 / 我没信过」
- [ ] 结束给分数，并逐条展开真相
- [ ] 生成一张成绩分享图（可复用 `scripts/generate-icons.mjs` 里的 sharp 管线）

**为什么**：没人会主动分享"一个辟谣网站"，但很多人会分享"我 10 条中了 7 条"。
这是这类内容最强的传播机制，而且把被动阅读变成了主动参与——
先让人承认自己信过，再给真相，接受度比直接说教高得多。

---

## A3 · 每条一张分享图 `M`（依赖 A1）

- [ ] 构建时批量生成 61 张 OG 图，图上就是那句被红笔划掉的话
- [ ] 配合 A1 的静态页面，各自注入自己的 `og:image`

现在不管分享哪一条，微信/Twitter 里出来的都是同一张站点封面。
做完之后每条链接都会展示自己的内容，分享点击率完全是两回事。

**必须先做完 A1**——没有独立页面就没法注入独立的 meta。

---

## A4 · 内容扩到 100 条 `L`

- [ ] 补齐分类不均衡

当前分布（共 61 条，其中 19 条标记「可能有害」）：

| 分类 | 条数 | |
|---|---|---|
| 吃 | 20 | 偏多了 |
| 身体 | 15 | |
| 生活 | 8 | |
| 关键时刻 | 7 | |
| 运动 | 6 | **需要补** |
| 睡 | 5 | **需要补** |

写新条目**必须遵守 HANDOVER 第 6 节的四条编辑原则**，尤其是
「区分证据不足和已被推翻」「有争议要留余地」这两条。
这个站的护城河是准确，不是数量——宁可 80 条扎实的，不要 120 条掺水的。

---

## A5 · 相关条目 `S` —— ✅ 已完成（`0fcedd2`）

- [x] 详情页底部加「你可能也以为……」，推同类或相关主题的 2–3 条
- [x] 在 `myths.ts` 里加可选字段 `related?: string[]`
- [x] 在 `myths.test.ts` 里加断言：`related` 里的 id 必须都存在、不能指向自己

现在读完一条就是死路，只能关掉回列表。这个能明显拉长停留时间，
而且很多条目天然成组（骨头汤补钙 ↔ 汤比肉有营养；感冒相关的好几条）。

---

## A6 · 纠错入口 `S` —— ✅ 已完成（`0fcedd2`）

- [x] 详情页加一个「这条写得不对」，点击跳 GitHub issue，URL 预填条目 id 和标题
- [x] 加 `.github/ISSUE_TEMPLATE/`：一个纠错模板、一个投稿新条目的模板

**为什么值得做**：这个站的立身之本是准确，而公信力是靠"**可以被纠错**"
建立的，不是靠自称严谨。把纠错入口放在最显眼的地方，本身就是一种态度表达。

---

## A7 · 出处链接体检 `M`

- [ ] 写个脚本跑一遍所有出处链接，报告失效的
- [ ] **必须用真实浏览器（Playwright），不能用 curl**
- [ ] 可以挂到 GitHub Actions 每月跑一次，发现失效自动开 issue

> ⚠️ 见 HANDOVER 坑①：Mayo Clinic、CDC、JAMA、BMJ、Cochrane 这些站
> 对 curl **一律返回 403，不管路径对不对**。用 curl 写这个脚本，
> 你会得到几十个假警报，然后可能误删掉一堆其实有效的链接。

---

## A8 · 性能与无障碍审计 `S`

- [ ] 跑一次 Lighthouse，四项都推到 90+
- [ ] 纯键盘走完整流程（Tab / Esc / ←→ / `/` 聚焦搜索）
- [ ] 用屏幕阅读器过一遍详情页
- [ ] 实测 `prefers-reduced-motion` 下划线动画是否正确降级
      （CSS 里写了规则，但一直没有真机验证过）

---

## 建议顺序

```
P0  发布手上的改动
 │
A1  改路由 + 预渲染      ← 动的是地基，越早越好
 │
A2  测验模式             ← 传播杠杆
 │
A3  每条分享图           ← 依赖 A1
 │
 ├── A5 相关条目 · A6 纠错入口 · A8 审计   （小而快，可穿插）
 └── A4 内容扩到 100 条                     （长期，慢慢加）
 └── A7 链接体检                            （做完 A4 再做更划算）
```

**如果只做三件事**：`P0` → `A1` → `A2`。
这三件做完，站点就从"一个能看的作品"变成"一个能持续长流量的产品"。

---

## 两条不能破的底线

1. **内容准确性不能省事。** HANDOVER 第 6 节那四条编辑原则是硬约束：
   区分"证据不足"和"已被推翻"、有争议留余地、每条给出处、不给医疗建议。
   页脚的免责声明不要删。

2. **提交前跑 `npm run check`。** CI 和 Netlify 是**相互独立**的——
   CI 失败**不会**阻止 Netlify 部署。所以本地这道检查是唯一的守门员。
